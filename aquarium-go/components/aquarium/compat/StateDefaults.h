#pragma once

// Fixed environment defaults. The retro-go aquarium app runs with
// FIXED_ENVIRONMENT, so these are the values the simulation always sees.
#define DEFAULT_TEMPERATURE_VALUE 22.0f
#define DEFAULT_CO2_VALUE 420.0f
